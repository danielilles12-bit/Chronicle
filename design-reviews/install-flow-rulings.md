# Install-flow rulings — Daniel, 5 Aug 2026 (all FINAL unless marked)

## Setup screen ("The Pass" direction, approved look)
- Headline: **SAVE YESTERNERD AS AN APP ON YOUR HOME SCREEN**
- Body (FINAL, Daniel's own words):
  "Once you save it as an app, tomorrow's games are one tap away. And a
  streak that lives in a browser can die in a browser — on your home
  screen, it's safe for good."
- Streak-risk claim stays UNIVERSAL and vague-but-true — NO per-browser
  deletion claims (the 7-day line is Safari-only; do not use it).

## Steps / affordances
- NO pointing arrows to browser chrome (position undetectable; wrong arrow
  is fatal; the card must teach the whole dance up front).
- Each step shows a DRAWN REPLICA of the exact real button/row, as exact as
  possible, rendered LARGER than the surrounding words — the eye lands on
  the button first.
- Step instructions must match the verified real flow per platform (e.g.
  Daniel's iPhone: Share → "View More" → scroll → [plus icon] "Add to Home
  Screen" — icon BEFORE label). Await workflow fact-check for per-version
  truth; instructions must be version-aware or version-safe.
- Browser-name dynamism: fine where trivially detectable, but no claims
  that depend on undetectable state.

## Timing
- Install ask: after **2 completed games** — any mix; one full game of any
  of FV/Relic/Thread/Lifeline + another full game of any INCLUDING an
  encore of the same game.
- Webview escape page ("Instagram can't keep apps"): after **1 completed
  game** — capture people out of social before they're lost.

## Webview escape page corrections
- The ⋯ location line must be platform-dynamic (Daniel's iPhone: top of
  screen, not bottom).
- Menu label is **"Open in external browser"** (NOT "Open in Safari").
- Use the correct glyph: square with an arrow pointing from inside toward
  the square's top-right corner.

## Process
- Model plan for any subagents requires Daniel's pre-approval (standing
  rule, in memory). Plan doc precedes any build; Daniel signs off first.

## Ground truth from Daniel's phone screenshots (5 Aug 07:20-07:21) — FINAL
- Share-sheet rows: ICON-LEFT, label-right. "Add to Home Screen" row = boxed
  [+] glyph then label. Reached via "View More" then scroll (his Safari) or
  directly in Chrome-iOS's sheet list.
- Daniel's Safari layout: bottom bar, Share visible center-bottom. Copy must
  ALSO serve Compact-default users ("behind •••") without contradicting.
- CHROME-iOS TRAP: Share icon is TOP-RIGHT next to the address bar; the ⋯
  bottom-right leads somewhere wrong — copy must explicitly warn "not the ⋯
  at the bottom".
- Instagram in-app browser (current iOS app): X top-left, ⋯ TOP-RIGHT.
  Menu: Link history / Send in Direct / Open in external browser (glyph:
  square with arrow exiting toward top-right) / Browser settings / Privacy
  Policy / Report website. Use "Open in external browser" VERBATIM for the
  Instagram variant; keep flexible phrasing for FB/TikTok/unknown.
- OG share preview card renders correctly (new square-glasses social card
  confirmed in the wild in Chrome's sheet).

## Daniel's post-build amendments (5 Aug, install screens approved otherwise)
1. The "View More" instruction gets the SAME text size as the other step
   instructions (no longer a small note).
2. Instagram escape page: an explicit "OR" between the two paths — steps
   (⋯ → Open in external browser) OR the COPY THE LINK button — so it reads
   as either/or.
3. Screen A header: use the SAME masthead as Home (the wordmark image), and
   replace the current Antinous art with a NATIVE-iOS-STYLE APP ICON of the
   app itself — icons/icon-512.png rendered with iOS rounded-corner icon
   geometry (~22% radius), soft shadow, NO zine ink border — because the
   screen's message is "this becomes an app".
4. Everything else approved as built.

# ---- FEEDBACK PLATE: APPROVED FOR BUILD (Daniel, 5 Aug 2026) ----
Direction: "Letters to the Editor" plate, letters-column rule (thick-over-thin),
British definitive-stamp language — NO envelope, no US iconography.
Mock (approved): scratchpad/site/feedback-mock-d.html + feedback-mock-d.png
- Kicker: LETTERS TO THE EDITOR (magenta plate)
- Headline: "Got opinions? Write in."
- Sub: "Praise, gripes, corrections, a clue that felt unfair — the editor
  reads everything."
- Button: "Write to the editor →" (ink, magenta hard shadow)
- Stamp: Antinous, perforated edges, **CYAN** face (--df-cyan), denomination
  is the LIVE ISSUE NUMBER ("№ 37" → dynamic), postmark carries the ACTUAL
  CURRENT DATE (dynamic) + YESTERNERD.
- "Tell us where it hurts" is NOT retired: reserve it for the win-screen
  rubber stamp (Direction B from the feedback plan).
Destination: Google Form (Daniel creates; placeholder URL until then) with
build + device appended, mailto fallback, GoatCounter event on tap.
Placement per the feedback plan's recommendation: Home foot + day-done moment
slot; plus quiet links (Home footer, Your Legacy, About).
